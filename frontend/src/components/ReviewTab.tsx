/**
 * A placeholder, so the navbar's shape is final before the feature exists.
 */
export function ReviewTab() {
  return (
    <div className="container section">
      <h1 className="title is-4">Review</h1>
      <p className="muted">
        This is where uploads will be looked over before they land in the repo.
        It is not built yet.
      </p>
    </div>
  )
}
